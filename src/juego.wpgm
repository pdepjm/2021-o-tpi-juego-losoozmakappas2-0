import wollok.game.*
import texto.*
import pokemon.*
import piedras.*

const pokemon = new Pokemon(ptosDeVida = 10, danio = 10, position = game.center())

program juego {
	game.title("Pokemon")
	game.width(20)
	game.height(20)
	game.addVisualCharacter(pokemon)
	game.addVisual(agua)
	game.addVisual(fuego)
	game.onCollideDo(pokemon, {
		piedra => pokemon.tocarPiedra(piedra)
	})
	game.start()
}