import wollok.game.*
import pokemon.*
import piedras.*
import fondos.*
import direcciones.*
import configuraciones.*
import niveles.*

program juego {
    game.title("Pokemon")
    game.width(15)
    game.height(10)
    nivel.cargarNivel(actual.nivel())
    nivel.configurarTeclas()
    
    game.start()
}