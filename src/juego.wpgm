import wollok.game.*
import pieces.*
import texto.*
import level.*

program juego {
	game.title("Tetris")
	game.cellSize(40)
	game.height(10)
	game.width(6)
	game.boardGround(background.image())
	level.keysConfig()
	game.start()
}